import React from 'react';
import { Formik, Field } from 'formik';
import { Button } from 'react-bootstrap';
import { v4 as uuidV4 } from 'uuid';


const Favorite = ({ onChange, selectedValue }) => {
    
    return (
        
        <p>Favorite Component</p>
        
    );
}

export default Favorite;